package jogo.modelo;

public enum CampoEvento {
	
	/*
	 * Eventos necessarios para quando acontecer
	 * um evento, notificar a ação para que ocorra
	 * em determinados momentos 
	 */
	ABRIR, MARCAR, DESMARCAR, EXPLODIR, REINICIAR;
}
